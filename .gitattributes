# Файлы служб и Caddyfile уезжают на Linux. С виндовыми переводами строк
# systemd подставляет \r в конец ExecStart и не находит исполняемый файл —
# ошибка выглядит как «команды не существует», хотя она на месте.
deploy/* text eol=lf
*.service text eol=lf
*.sh text eol=lf
