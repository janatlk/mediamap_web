# MediaMap

Карта нарушений в медиапространстве Кыргызстана: язык вражды, дезинформация
и пропаганда. Заявки подаются публично и попадают на карту после проверки
модератором.

Это новая версия сайта. От прежней взяты структура разделов, тексты и
данные; интерфейс, схема базы и серверная часть написаны заново.

## Запуск

```bash
npm install
npx prisma migrate dev
npm run db:seed
npm run dev
```

Сайт поднимется на `http://localhost:3000`.

Переменные окружения — в `.env`:

```
DATABASE_URL="file:./prisma/dev.db"
SESSION_SECRET="…"
```

## Стек

| Слой | Решение | Почему |
|---|---|---|
| Каркас | Next.js 16, App Router | Серверный рендеринг: страницы нарушений и новостей должны попадать в поиск |
| Стили | Tailwind CSS 4 | Токены задаются в CSS, отдельного конфига нет |
| База | Prisma 7 + SQLite | На сервере переедет на Postgres, схема к этому готова |
| Шрифты | Geologica, Golos Text, JetBrains Mono | Кириллические по происхождению — важно для кыргызского |

## Устройство

```
prisma/
  schema.prisma      схема базы
  seed.ts            наполнение из seed-data.json
scripts/
  prepare-regions.py готовит геометрию областей
  export-legacy.py   выгружает данные прежнего сайта
  check-contrast.py  проверяет палитру по WCAG
src/
  app/globals.css    дизайн-система: цвета, размеры, шрифты
  lib/               база, словарь текстов, проекция карты
  components/        шапка, подвал, карта главной
```

## Дизайн-система

Все цвета и размеры живут в `src/app/globals.css`. Правило простое: если
нужного значения нет в токенах, решение ещё не принято — принимать его надо
там, а не дописывать число в разметке.

Контраст палитры проверяется скриптом:

```bash
npm run check:contrast
```

Замеры на готовой главной: текста мельче 12px — 0%, начертаний два (400 и
500), цветов текста пять, провалов контраста нет, на экране 375px нет
горизонтальной прокрутки, все цели нажатия не меньше 24px.

## Данные карты

Границы областей — [geoBoundaries](https://www.geoboundaries.org/), набор
gbOpen KGZ ADM1, построен по данным OpenStreetMap, лицензия ODbL 1.0.
Лицензия требует указания источника — ссылка стоит в подвале сайта.

Исходный файл упрощается скриптом до 17 КБ; площадь при этом меняется на
0.04% по стране.

Тонкость, на которой легко споткнуться: d3-geo считает геометрию на сфере,
и направление обхода кольца решает, какую часть сферы считать внутренней.
Конвенция у него обратна RFC 7946 — внешнее кольцо идёт по часовой стрелке.
Порядок точек приводится к нужному в `scripts/prepare-regions.py`.

## Что ещё не сделано

Готова главная страница. Остальные разделы, форма подачи заявки, админка и
вход — впереди.

Кыргызские строки в `src/lib/content.ts`, добавленные заново, нуждаются в
вычитке носителем языка.
